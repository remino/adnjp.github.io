---
layout: blank
---
div lang="ja"
  h1 = t 'title', locale: :ja

  p
    = link_to t('locale.ja'), '/ja/', class: 'btn btn-default',
      'data-langsel-locale' => 'en'

div lang="en"
  h1 = t 'title', locale: :en

  p
    = link_to t('locale.en'), '/en/', class: 'btn btn-default',
      'data-langsel-locale' => 'ja'

- content_for :foot do
  = javascript_include_tag :langsel
